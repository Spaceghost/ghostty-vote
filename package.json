{
  "name": "ghostty-vote",
  "private": true,
  "type": "module",
  "description": "Public feature vote for the Ghostty for FFXIV plugin: static page on Workers Static Assets, a tiny API Worker and D1.",
  "scripts": {
    "build": "node scripts/build.js",
    "check": "node scripts/build.js --check",
    "test": "node --test"
  }
}
