name = "ghostty-vote"
main = "src/worker.js"
compatibility_date = "2026-09-01"

# Only reachable through the zone route below: no *.workers.dev or preview URLs,
# so the path-scoped Secure cookie always matches.
workers_dev = false
preview_urls = false

# Exactly this path prefix on spacegho.st; every other route on the zone is untouched.
routes = [
  { pattern = "spacegho.st/mods/ffxiv/term/vote*", zone_name = "spacegho.st" }
]

[[d1_databases]]
binding = "DB"
database_name = "ghostty-vote"
database_id = "REPLACE_WITH_DATABASE_ID"
migrations_dir = "migrations"

# Import src/page.html as a string.
[[rules]]
type = "Text"
globs = ["**/*.html"]
fallthrough = true
